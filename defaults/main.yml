ansible_collections:
  community:
    - name: community.general
      version: "'*'"
  github:
  git:

ansible_roles:
  community:
  git:
    - name: git+https://github.com/aklambeth/ansible-role-ansible.git
      version: master
  github:
    

ansible_install:
  # - ansible
  - collections

target_collection_folder: default # local
target_role_folder: default # local
requirements_file_folder: "~/"